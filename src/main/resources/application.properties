spring.application.name=SE2034-SWP391-G4
server.port=8080 

#
#
#spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=ElearningPlatform;encrypt=true;trustServerCertificate=true
#spring.datasource.username=sa
#spring.datasource.password=123

spring.datasource.url=${LOCAL_SQL_SERVER_URL:jdbc:sqlserver://localhost:1433;databaseName=ElearningPlatform;encrypt=true;trustServerCertificate=true}
spring.datasource.username=${LOCAL_SQL_SERVER_USERNAME:sa}
spring.datasource.password=${LOCAL_SQL_SERVER_PASSWORD:123}

# Phần kết nối DB dành cho Deploy
#spring.datasource.url=${AZURE_SQL_SERVER_URL}
#spring.datasource.username=${AZURE_SQL_SERVER_USERNAME}
#spring.datasource.password=${AZURE_SQL_SERVER_PASSWORD}

spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=true
server.servlet.session.persistent=false
spring.servlet.multipart.max-file-size=150MB
spring.servlet.multipart.max-request-size=150MB

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.thymeleaf.cache=false 
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

#Google config
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email

#Mail config
spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}

spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# PayOS
payos.client-id=${PAYOS_CLIENT_ID}
payos.api-key=${PAYOS_API_KEY}
payos.checksum-key=${PAYOS_CHECKSUM_KEY}

# Security Config
app.security.remember-me.key=${REMEMBER_ME_SECRET_KEY}


